package simpleModel.simdata.input;

import kr.ac.kaist.se.simdata.input.SimConfiguration;

public class SimpleConfiguration extends SimConfiguration {
    public SimpleConfiguration(int time) {
        super();
        this.totalTimeFrame = time;
    }
}
